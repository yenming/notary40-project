{
  "name": "store-radar",
  "version": "0.1.0",
  "description": "Init Project MVC application with Express, EJS, Sequelize, and Bootstrap.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "node --test",
    "build:css": "sass public/scss:public/css --style=compressed --no-source-map",
    "watch:css": "sass --watch public/scss:public/css",
    "db:migrate": "sequelize-cli db:migrate",
    "db:seed": "sequelize-cli db:seed:all",
    "db:undo": "sequelize-cli db:migrate:undo",
    "db:reset": "sequelize-cli db:migrate:undo:all && sequelize-cli db:migrate && sequelize-cli db:seed:all",
    "db:migrate:docker": "docker compose exec app npx sequelize-cli db:migrate",
    "db:seed:docker": "docker compose exec app npx sequelize-cli db:seed:all",
    "db:reset:docker": "docker compose exec app sh -c \"npx sequelize-cli db:migrate:undo:all && npx sequelize-cli db:migrate && npx sequelize-cli db:seed:all\""
  },
  "keywords": [
    "express",
    "mvc",
    "ejs",
    "sequelize",
    "mysql",
    "bootstrap",
    "sass",
    "docker"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cookie-parser": "^1.4.7",
    "dotenv": "^16.4.5",
    "ejs": "^3.1.10",
    "express": "^4.19.2",
    "express-ejs-layouts": "^2.5.1",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "mysql2": "^3.11.3",
    "sequelize": "^6.37.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.7",
    "sass": "^1.80.3",
    "sequelize-cli": "^6.6.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

