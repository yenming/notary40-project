# Server
PORT=3000
NODE_ENV=development
DB_CONNECT_RETRIES=15
DB_CONNECT_RETRY_DELAY=2000

# Database (local host defaults; Docker Compose app service overrides internally)
DB_HOST=127.0.0.1
DB_PORT=3307
DB_NAME=notary40
DB_USER=notary40_app
DB_PASSWORD=notary40_pass
DB_ROOT_PASSWORD=root_super_secret

# Security
JWT_SECRET=supersecretjwtkey
JWT_EXPIRES_IN=1d
BCRYPT_SALT_ROUNDS=10
